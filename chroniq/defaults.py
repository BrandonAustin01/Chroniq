# chroniq/defaults.py

# These are internal, hardcoded fallback values
DEFAULT_CONFIG = {
    "silent": False,
    "strict": False,
    "emoji_fallback": True,
    "default_bump": "patch",
    "active_profile": "default"
}
